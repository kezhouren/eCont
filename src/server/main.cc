#include <stdio.h>
#include <vector>


//using namespace std;
int main(int argc, char** argv)
{
    char key;
    std::vector<Record*> list;
    print_help(); 
    init_data(100, list);
    while(1)
    {
    }
    return 0;
}

